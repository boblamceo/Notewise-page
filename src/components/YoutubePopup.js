import { Dialog } from "@mui/material";
import React from "react";

const YoutubePopup = ({ open }) => {
    return (
        <Dialog open={open}>
            <div>hi</div>
        </Dialog>
    );
};

export default YoutubePopup;
